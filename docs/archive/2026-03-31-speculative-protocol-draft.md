# Speculative Protocol Draft - 2026-03-31

## Completed In This Node

- added `docs/project/speculative-decoding-protocol-draft.md`
- defined the first message set, session model, acceptance rules, diagnostics rules, and fallback behavior
- updated project status so the next active stage is implementing the first speculative session flow

## Why This Matters

- the speculative layer now has a concrete protocol draft instead of only a long-term concept
- future implementation work can target explicit messages and state transitions
- the already proven local and ordinary remote paths remain the fallback baselines while speculation is introduced

## Recommended Resume Point

Resume from `docs/project/current-status.md`, then implement the first desktop-side speculative session endpoints using `docs/project/speculative-decoding-protocol-draft.md` as the reference.
